metadata:
  template: aishield-static-web-app
name: aishield
plan: basic
resourceGroup: rg-aishield

variables:
  AZURE_LOCATION: eastus
  AZURE_SUBSCRIPTION_ID: ${AZURE_SUBSCRIPTION_ID}

services:
  web:
    project: .
    language: html
    host: staticwebapp
